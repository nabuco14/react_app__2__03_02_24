import Contenido from "../contenido/contenido";
import Contenido2 from "../contenido2/contenido2";

export default function Gestor({opcion}){
    switch(opcion){
        case 0: return(Contenido)

        case 1: return(Contenido2)

    }        
}